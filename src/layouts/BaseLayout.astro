---
import '../styles/index.scss';
import Header from '../components/Header.astro';
import Toast from '../components/Toast.astro';
import type { AuthUser } from '../lib/auth';
import type { FlashMessage } from '../lib/error-handler';
import styles from './BaseLayout.module.scss';

export interface BaseLayoutProps {
  title?: string;
  description?: string;
  user: AuthUser | null;
  currentPath: string;
  flash?: FlashMessage | null;
}

const {
  title = 'TAMSAN Lab | Labが行く',
  description = 'TAMSAN Labのインタビュー記事ポータルサイト',
  user,
  currentPath,
  flash = null,
}: BaseLayoutProps = Astro.props;
---

<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class={styles.page}>
    <Header user={user} currentPath={currentPath} />
    <main class={styles.main}>
      <slot />
    </main>
    {flash && <Toast message={flash.message} type={flash.type} />}
  </body>
</html>
